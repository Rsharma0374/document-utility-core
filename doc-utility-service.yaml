apiVersion: v1
kind: Service
metadata:
  name: doc-utility-service
spec:
  type: NodePort
  selector:
    app: doc-utility-service
  ports:
    - protocol: TCP
      port: 10005
      targetPort: 10005
      nodePort: 30005